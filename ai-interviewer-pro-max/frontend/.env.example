# ===========================================
# AI Interviewer Pro Max - Frontend Environment
# ===========================================
#
# ⚠️  NOTE: The frontend does NOT handle API keys directly!
# All AI API calls go through the backend for security.
#
# Copy this file to .env.local for local development overrides.
#
# ===========================================

# ===========================================
# API CONFIGURATION
# ===========================================
# The base URL for the backend API
# In development, Vite proxy handles this (see vite.config.js)
# In production, set this to your backend URL
VITE_API_BASE_URL=http://localhost:8000

# ===========================================
# FEATURE FLAGS (Optional)
# ===========================================
# Enable/disable features in development
VITE_ENABLE_DEBUG_MODE=false
VITE_ENABLE_MOCK_DATA=false

# ===========================================
# SECURITY NOTE
# ===========================================
# ⚠️ NEVER put API keys or secrets in frontend environment variables!
# - Frontend code is visible to users
# - All VITE_ prefixed variables are bundled into the client
# - Use backend proxy for all API calls
